name: Hello World Workflow

on:
  push:         
    branches:
      - main   

jobs:
  say-hello:
    runs-on: ubuntu-latest  

    steps:
    - name: Print a greeting
      run: echo "Hello, world!"
